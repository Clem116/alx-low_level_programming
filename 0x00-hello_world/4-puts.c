#include <stdio.h>
/**
 * main - Entry point
 * Description: 'The program's that prints a line with puts function'
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("Programming is like building a multiling puzzle");
return (0); /*Returns 0 Value*/
}
